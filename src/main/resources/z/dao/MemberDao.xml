<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="z.dao.MemberDao">

  <resultMap type="member" id="member-map">
    <id     column="mnum"       property="memberNo"/>
    <result column="email"      property="email"/>
    <result column="name"       property="name"/>
    <result column="pwd"        property="password"/>
    <result column="photo"      property="photo"/>
    <result column="kakao"      property="kakao"/>
    <result column="fcbk"       property="facebook"/>
  </resultMap>

  <select id="getListMeetingMemb" resultMap="member-map">
    select * from memb join link on memb.mnum=link.mnum where mtnum=1;
  </select>
  
  <select id="getList" resultMap="member-map">
    select * from memb;
  </select>
  

  <select id="getOne" parameterType="int" resultMap="member-map">
    select *
    from memb
    where mnum=#{value};
  </select>
  
    <select id="getOneByEmail" parameterType="string" resultMap="member-map">
    select 
      mnum, 
      name, 
      email
    from memb
    where email=#{value}
  </select>
  
  <insert id="insert" parameterType="member" 
          useGeneratedKeys="true" keyColumn="mnum" keyProperty="memberNo">
    insert into memb(email, name, pwd, photo, kakao, fcbk)
    values(#{email}, #{name}, password(#{password}), #{photo}, #{kakao}, #{facebook})
  </insert> 

  <select id="count" parameterType="string" resultType="int">
    select count(*) from memb where email=#{value}
  </select>
  
  <select id="getOneByEmailPassword" parameterType="map" resultMap="member-map">
    select 
      mnum, 
      name, 
      email,
      photo,
      kakao,
      fcbk
    from memb
    where email=#{email} 
      and pwd=password(#{password})
  </select>
  
  <!-- 
  
  <update id="update" parameterType="member">
    update memb set
      email=#{email}, 
      pwd=password(#{password}), 
      name=#{name}, 
      tel=#{tel}
    where mno=#{memberNo}
  </update> 

  <delete id="delete" parameterType="int">
    delete from memb where mno=#{value}
  </delete>
   -->

</mapper>